"use client";
import React from "react";
import * as _Builtin from "./_Builtin";
import * as _utils from "./utils";
import _styles from "./Tab4Paragraph.module.css";

export function Tab4Paragraph({
  as: _Component = _Builtin.Paragraph,
  paragraph = "Efficiently apply foundation models to automatically add metadata to your visual data using state-of-the-art models from Visual Layer, open-source, or third-party APIs. Focus only on the data that matters, saving 98% of the costs.",
}) {
  return (
    <_Component className={_utils.cx(_styles, "is-body-m")}>
      {
        "Efficiently apply foundation models to automatically add metadata to your visual data, using state-of-the-art models from Visual Layer, open-source, or third-party APIs. Focus only on the data that matters, saving 98% of the cost."
      }
    </_Component>
  );
}
